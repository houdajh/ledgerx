spring:
  application:
    name: identity-service
  cloud:
    config:
      uri: http://localhost:8888  # your Config Server
      fail-fast: true

# Optional logging or profile info
logging:
  level:
    root: INFO
